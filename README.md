# ctsSubtypeR

`ctsSubtypeR` classifies new samples into consensus transcriptomic subtypes (CTS)
using packaged reference gene expression data.

## Installation

```r
# install.packages("remotes")
remotes::install_github("bpscicluna/ctsSubtypeR")# ctsSubtypeR

`ctsSubtypeR` classifies new samples into consensus transcriptomic subtypes (CTS)
using packaged reference gene expression data.

## Installation

```r
# install.packages("remotes")
remotes::install_github("bpscicluna/ctsSubtypeR")

